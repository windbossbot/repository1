services:
  - type: worker
    name: krx-filter-worker
    runtime: python
    buildCommand: "python -m pip install -r requirements.txt"
    startCommand: "python main.py | python ma_filter.py"
    envVars:
      - key: DATA_GO_KR_SERVICE_KEY
        sync: false
